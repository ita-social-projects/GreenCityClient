import { CorrectUnitPipe } from './correct-unit.pipe';

describe('CorrectUnitPipe', () => {
  it('create an instance', () => {
    const pipe = new CorrectUnitPipe();
    expect(pipe).toBeTruthy();
  });
});
