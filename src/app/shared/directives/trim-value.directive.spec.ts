import { TrimValueDirective } from './trim-value.directive';

describe('TrimValueDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimValueDirective();
    expect(directive).toBeTruthy();
  });
});
