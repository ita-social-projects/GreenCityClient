import { LocalizedCurrencyPipe } from './localized-currency.pipe';

describe('LocalizedCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new LocalizedCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
