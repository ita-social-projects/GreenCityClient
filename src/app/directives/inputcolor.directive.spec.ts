import { InputcolorDirective } from './inputcolor.directive';

describe('InputcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new InputcolorDirective();
    expect(directive).toBeTruthy();
  });
});
