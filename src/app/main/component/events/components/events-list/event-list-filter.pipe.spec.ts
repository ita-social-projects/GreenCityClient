import { EventListFilterPipe } from './event-list-filter.pipe';

describe('EventListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EventListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
