import { UncheckedFirstPipe } from './unchecked-first.pipe';

describe('UncheckedFirstPipe', () => {
  it('create an instance', () => {
    const pipe = new UncheckedFirstPipe();
    expect(pipe).toBeTruthy();
  });
});
