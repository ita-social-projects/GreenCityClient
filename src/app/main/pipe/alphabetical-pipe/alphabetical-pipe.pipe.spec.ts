import { AlphabeticalPipePipe } from './alphabetical-pipe.pipe';

describe('AlphabeticalPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AlphabeticalPipePipe();
    expect(pipe).toBeTruthy();
  });
});
